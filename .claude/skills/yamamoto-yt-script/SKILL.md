---
name: yamamoto-yt-script
description: >-
  山本さんRAGデータとX(Twitter)リサーチを組み合わせて、テーマに沿ったYouTube台本（30分構成+Shorts3本）を生成するスキル。
  Use when the user mentions YouTube台本、YT台本、動画台本、YouTube script、台本作成,
  or asks to create YouTube scripts based on a theme.
  Also triggered when continuing from yamamoto-x-post with "YouTube台本も作る".
---

# 山本さん YouTube台本生成スキル

テーマを受け取り、Xリサーチ（任意）→ 台本構成 → RAG検索 → YouTube台本生成 を行う。
各ステップでユーザーの承認を得てから次に進む。
承認は必ず **AskUserQuestionツール** を使い、選択肢付きで確認すること。

> **X記事スキルからの連携時:** yamamoto-x-postスキルで「YouTube台本も作る」を選択した場合、
> すでにテーマ・Xリサーチ結果・記事構成・RAGデータが会話コンテキストにある。
> その場合はステップ①〜④をスキップし、ステップ⑤に直行する。
> 「X記事作成で収集したリサーチ・RAGデータを引き継ぎます。YouTube台本生成に進みます。」と表示してからステップ⑤を開始すること。

## 設定

- **山本さんフォルダ（現役）**: `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/`
- **使用済みフォルダ**: `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/used/`
- **使用履歴ログ**: `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/used/_USAGE_LOG.md`
- **RAG運用ポリシー**: `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/RAG-POLICY.md`（ステップ④開始時に必ずReadで読み込む）
- **台本プロンプトファイル**: `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/insights/video_prompt/Louis Gleeson氏プロンプト.md`
- **Xリサーチ**: デフォルトON（ユーザーが「Xリサーチなし」「リサーチスキップ」等と言った場合はOFF）

### RAG運用の原則（要約）
1. RAGデータは広く取得（候補8〜12件）、反映は少数に絞る（**YouTube台本は2〜3件**：体験談枠＋実践枠）
2. 1本の動画に複数の具体事例を過剰に混在させない。テーマ一貫性が最優先
3. 反映に使った事例は `used/` へ移動し、`_USAGE_LOG.md` にNo.付きで記録
4. `_USAGE_LOG.md` の **末尾10エントリ** で使われた事例は今回の採用候補から降格
5. 永久除外はしない。10投稿を超えたものは再利用可（ただし切り口・論点・表現を変える）

---

## ステップ① テーマ受け取り & 初回セットアップ案内

ユーザーからテーマを受け取る。
最初に X API の設定状況をチェックし、未設定なら案内を表示する。

**やること:**

### 1. X API セットアップチェック
Bashツールで `source ~/.bashrc 2>/dev/null; echo $X_API_BEARER_TOKEN` を実行し、環境変数の設定状況を確認する。

**未設定の場合、以下の案内を表示する:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  X API セットアップのご案内（任意・初回のみ）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

X APIを設定すると、Xの最新投稿をより正確にリサーチできます。
未設定でも動作しますが、リサーチ精度が下がる場合があります。

【設定する場合の手順】（所要時間: 約10分）

  STEP 1: 旧Developer Portalでアカウント作成 & Bearer Token取得
  https://developer.x.com
  → サインイン → 利用目的を入力（250文字以上）→ 送信
  → 「Projects & Apps」→ 自分のApp → 「Keys and tokens」タブ
  → 「Bearer Token」の「Generate」→ トークンをコピー

  STEP 2: 新Developer ConsoleでPay-Per-Useプランに切り替え
  https://console.x.com （旧Portalとは別の画面です）
  → 左メニュー「アプリ」→ Appが「Free」なら「Pay-Per-Use」に変更
  ※ Freeのままだと検索APIで403エラーになります

  STEP 3: クレジット購入 & 支出上限設定
  → 左メニュー「請求書作成」→「クレジット」
  → 「クレジットを購入する」→ 最低 $5（約750円）をチャージ
  → 「支出上限を管理」→ 上限を $5 に設定（重要！デフォルトは無制限）

  STEP 4: ターミナルで環境変数を保存
  echo 'export X_API_BEARER_TOKEN="STEP1でコピーしたトークン"' >> ~/.bashrc
  source ~/.bashrc

  STEP 5: 動作確認
  echo $X_API_BEARER_TOKEN
  → トークンが表示されればOK

【参考リンク】
  詳しい手順: https://www.and-and.co.jp/gas-lab/x-twitter-api-start/
  503エラー解決: https://www.and-and.co.jp/gas-lab/x-twitter-api-503-error/

【今は設定しない場合】
  → そのまま進めます（WebSearch + Nitter でリサーチします）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```

→ AskUserQuestionツールで確認:
  question: "X APIを設定しますか？"
  header: "X API"
  options:
    - label: "設定する"
      description: "X Developer Portalで設定を行います（約10分）"
    - label: "今はスキップ"
      description: "WebSearch + Nitter でリサーチします（APIなしでも動作します）"

**設定済みの場合:**
```
X API: 設定済み（フル機能でリサーチできます）
```

### 2. テーマとリサーチ設定の確認
1. テーマを確認する
2. テーマ・Xリサーチ設定・X API状態をまとめて表示した上で、AskUserQuestionツールで確認:
  question: "テーマ「{テーマ}」/ Xリサーチ: {ON/OFF} / X API: {設定済み/未設定} でYouTube台本を作成しますか？"
  header: "テーマ確認"
  options:
    - label: "このまま進める（推奨）"
      description: "上記の設定でステップ②に進みます"
    - label: "テーマを変更"
      description: "別のテーマに変更します"
    - label: "Xリサーチをオフに"
      description: "Xリサーチを省略してRAG検索から始めます"

**承認を得てから次へ進む。**

---

## ステップ② Xで最新情報をリサーチ

> Xリサーチ=OFFの場合はこのステップをスキップし、ステップ③も省略してステップ④へ進む。

**やること:**

3段階のフォールバック方式でXの最新情報を収集する。
各Phaseで十分な結果（具体的な投稿内容を含む3件以上）が得られたら、次のPhaseはスキップする。

### Phase 1: WebSearchで検索（無料）
1. WebSearchツールを使い、以下の検索クエリでXの最新投稿・トレンドを調査する:
   - `{テーマ} site:x.com`
   - `{テーマ} Twitter トレンド`
   - テーマに関連するキーワードの組み合わせ（2〜3パターン）
2. **判定:** 具体的な投稿内容を含む結果が3件以上 → Phase 2, 3をスキップ

### Phase 2: Nitterでフォールバック補完（無料）
3. Phase 1の結果が不十分な場合、Nitter経由で追加リサーチする:
   - WebFetchツールで `https://xcancel.com/search?q={テーマ}&f=tweets` を取得
   - 取得できない場合は `https://twiiit.com/search?q={テーマ}&f=tweets` を試す
   - それでも取得できない場合は `https://nitter.privacydev.net/search?q={テーマ}&f=tweets` を試す
4. **判定:** Phase 1 + 2 の合計で3件以上 → Phase 3をスキップ

### Phase 3: X API で最終補完（従量課金）
5. Phase 1 + 2 でも情報が不十分な場合、X APIで検索する:
   - 環境変数 `X_API_BEARER_TOKEN` が設定されているか確認
   - 未設定の場合: ユーザーに「X APIが未設定のため、現在の情報で進めます」と通知し、手持ちの情報で進める
   - 設定済みの場合: 以下のコマンドでツイート検索を実行
   ```bash
   curl -s -H "Authorization: Bearer ${X_API_BEARER_TOKEN}" \
     "https://api.x.com/2/tweets/search/recent?query={テーマ}&max_results=20&tweet.fields=created_at,public_metrics,text" \
     | python3 -c "
   import json, sys
   data = json.load(sys.stdin)
   for tweet in data.get('data', []):
       print(f\"[{tweet.get('created_at','')}] {tweet['text']}\")
       metrics = tweet.get('public_metrics', {})
       print(f\"  likes: {metrics.get('like_count',0)}, RT: {metrics.get('retweet_count',0)}\")
       print()
   "
   ```

6. 全Phaseの結果を統合し、以下のフォーマットでまとめる:

```
## Xリサーチ結果

情報ソース: {WebSearch / Nitter / X API}（使用したPhaseを記載）

### トレンド・話題
- {箇条書きで3〜5件}

### 注目の意見・切り口
- {箇条書きで3〜5件}

### 使えそうなフレーズ・表現
- {箇条書きで3〜5件}

### 高エンゲージメント投稿（X API使用時のみ）
- {いいね・RT数が多い投稿の要約}
```

7. リサーチ結果を表示した上で、AskUserQuestionツールで確認:
  question: "Xリサーチ結果を確認してください。このまま進めますか？"
  header: "リサーチ確認"
  options:
    - label: "このまま進める（推奨）"
      description: "この結果をベースにステップ③へ進みます"
    - label: "追加で調べてほしい"
      description: "キーワードや切り口を追加してリサーチし直します"
    - label: "リサーチ結果を使わない"
      description: "Xリサーチを省略してRAG検索のみで進めます"

**承認を得てから次へ進む。**

---

## ステップ③ 台本構成に整理

**やること:**
1. ステップ②のリサーチ結果を、RAGに投げやすい台本構成に整理する:

```
## 台本構成

### テーマ: {テーマ}

### 背景・トレンド
{Xリサーチから得たトレンド情報を2〜3文で要約}

### 主要な論点
1. {論点1}
2. {論点2}
3. {論点3}

### ターゲット視聴者の悩み
- {悩み1}
- {悩み2}
- {悩み3}

### 動画の方向性
{どういう切り口で台本を書くべきかの方向性。前半＝考え方、後半＝実践のイメージ}
```

2. 台本構成を表示した上で、AskUserQuestionツールで確認:
  question: "この台本構成でRAGに投げますか？"
  header: "構成確認"
  options:
    - label: "このまま進める（推奨）"
      description: "この構成でRAG検索を実行します"
    - label: "構成を修正"
      description: "論点やターゲットの悩みを調整します"
    - label: "構成をスキップ"
      description: "リサーチ結果をそのままRAGに投げます"

**承認を得てから次へ進む。**

---

## ステップ④ 山本さんRAGに投げる（取得フェーズ → 反映フェーズの2段階）

> **重要:** このステップは「取得は広く、反映は絞る」を徹底する。
> 開始時に必ず `山本さん/RAG-POLICY.md` をReadツールで読み込み、内容に従うこと。

### 4-0. ポリシー読込と使用履歴チェック

1. **Readツール**で `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/RAG-POLICY.md` を読み込む
2. **Readツール**で `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/used/_USAGE_LOG.md` を読み込む
3. ログ末尾の **直近10エントリ** で使用された事例ファイル名を抽出し「クールダウン対象リスト」を作る（初回や10件未満の場合はあるだけでよい）

### 4-1. 取得フェーズ（広く集める）

目的: テーマに沿った山本さんの知見・引き出しを幅広く把握する。

**具体的な手順:**
1. Grepツールでテーマのキーワードを検索する。検索対象は以下の **両方**:
   - `山本さん/`（現役ファイル）
   - `山本さん/used/`（アーカイブも含める。永久除外しないため）
2. 関連性の高いファイルを **8〜12件** 読み込む
3. 抽象的な知見・方向性の材料として自由に参照してよい
4. 以下のフォーマットで「候補プール」を作成:

```
## RAG候補プール（取得フェーズ）

### 参照した知見・方向性（自由に活用してよい）
- {テーマに関する山本さんの考え方を3〜5件}

### 具体事例の候補リスト（反映フェーズで絞り込む）
| 候補No | 元ファイル | 事例の要点 | クールダウン | テーマ整合度 |
|--------|-----------|-----------|-------------|-------------|
| 1 | {ファイル名}.txt | {数字・固有名詞を含む1行要約} | {該当あり／なし} | {高／中／低} |
| 2 | ... | ... | ... | ... |
| ... | ... | ... | ... | ... |
```

- **クールダウン**: 4-0で抽出した直近10エントリに含まれるファイルは「該当あり」と明記
- **テーマ整合度**: Xリサーチ結果（ある場合）と照らし、テーマに最も沿うものを「高」とする

### 4-2. 反映フェーズ（少数に絞る）

目的: YouTube台本本編にねじ込む具体事例を **2〜3件** に厳選する。

**具体的な手順:**
1. YouTube台本は30分構成・二部構成のため、以下の枠に合わせて事例を配置:
   - **体験談枠（SECTION 6: 11:00〜13:00）**: 1件（最重要の事例をここで深掘り）
   - **実践・How-to枠（SECTION 8: 13:30〜22:00）**: 1〜2件（具体例として差し込む）
2. 候補プールから、以下の優先順で採用候補を並び替える:
   - 第1優先: クールダウン該当なし × テーマ整合度「高」
   - 第2優先: クールダウン該当なし × テーマ整合度「中」
   - 第3優先: クールダウン該当あり だが テーマ整合度「高」で代替不可の場合のみ（切り口を変える前提）
3. 上位 **2〜3件** を「反映候補」として選ぶ（原則2件）
4. 以下のフォーマットでまとめる:

```
## RAG反映プラン

### 採用する具体事例（2〜3件）
1. **{ファイル名}.txt** — 配置: SECTION 6（体験談）
   - 使う切り口: {テーマに対してどう位置づけるか}
   - 使う数字・固有名詞: {具体的に書き起こす}
   - クールダウン: {該当なし／該当あり（前回使用: No.X, 切り口を変える旨）}
2. **{ファイル名}.txt** — 配置: SECTION 8（実践）
   - 使う切り口: ...
   - 使う数字・固有名詞: ...
   - クールダウン: ...

### 台本に使えるキーメッセージ（抽象側・引き出しから）
- {メッセージ1}
- {メッセージ2}
- {メッセージ3}

### 今回「採用しない」理由を記録した候補
- {ファイル名}.txt: {採用しない理由。例: クールダウン該当 / テーマとズレる}
```

5. 反映プランを表示した上で、AskUserQuestionツールで確認:
  question: "採用する具体事例は {件数} 件の予定です。この絞り込みで台本を作成しますか？"
  header: "反映事例の確認"
  options:
    - label: "このまま進める（推奨）"
      description: "この2〜3件に絞って台本を生成します"
    - label: "別の事例に差し替え"
      description: "候補プールから別のファイルを採用し直します"
    - label: "1件に絞る"
      description: "体験談1件のみで台本を作成します（実践枠は抽象で対応）"
    - label: "もっと候補を集めたい"
      description: "取得フェーズに戻り、別キーワードで追加検索します"

**承認を得てから次へ進む。**

---

## ステップ⑤ YouTube台本を生成

**やること:**
1. ステップ②〜④で集めた情報をすべて統合する
2. **Readツール**で台本プロンプトファイルを読み込む:
   - `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/insights/video_prompt/Louis Gleeson氏プロンプト.md`
   - このファイルには以下が含まれている:
     - **BRAND DNA**: 山本智也の話し方・思考法・口調パターン
     - **YouTube台本テンプレート**: 30分構成・12セクション（フック〜CTA）
     - **離脱防止設計**: 5分ごとのつなぎフレーズ
     - **サムネイル＆タイトル生成ルール**
     - **Shorts台本テンプレート**: 3パターン（数字インパクト型・How-to型・共感→転換型）
     - **出力フォーマット**: 統一フォーマット
     - **品質チェックリスト**: 21項目

3. 読み込んだプロンプトに**忠実に**従い、以下の成果物を一括生成する:

### 生成する成果物

**A. タイトル案（3パターン）**
- プロンプトのタイトル生成ルールに従う
- 28〜40文字、数字を1つ以上、SEOキーワードを前半に
- パターンA〜Eのいずれかで生成

**B. サムネテキスト案（5パターン）**
- 10〜18文字、数字を大きく見せる
- 感情ワードを入れる（「衝撃」「激変」「完全版」等）
- 色指示を含める

**C. 動画構成表（30分）**
- 12セクションの時間・内容一覧表

**D. 台本本文（9,000〜10,000文字）**
- プロンプトの12セクション構成に忠実に従う:
  - SECTION 1: フック（0:00〜0:20）
  - SECTION 2: テーマ発表＋自己紹介＋全体像（0:20〜1:30）
  - SECTION 3: 問題提起＋背景解説（1:30〜5:00）
  - SECTION 4: 中間まとめ①（5:00〜5:30）
  - SECTION 5: 本編パート1 — 概念・考え方（5:30〜11:00）
  - SECTION 6: 体験談・エピソード（11:00〜13:00）
  - SECTION 7: 中間まとめ②＋後半予告（13:00〜13:30）
  - SECTION 8: 本編パート2 — 実践・How-to（13:30〜22:00）
  - SECTION 9: Q&A・よくある質問（22:00〜25:00）
  - SECTION 10: 応用・発展（25:00〜27:00）
  - SECTION 11: 全体まとめ（27:00〜28:30）
  - SECTION 12: CTA＋次回予告（28:30〜30:00）
- 各セクションに [テロップ案] [画面切替指示] を付ける
- すべて**話し言葉**で書く（書き言葉厳禁）
- BRAND DNAの口調パターンを忠実に再現する

**E. Shorts台本（3本）**
- Shorts①: 数字インパクト型（60秒）
- Shorts②: How-to型（60秒）
- Shorts③: 共感→転換型（60秒）

### 台本生成時の重要ルール

1. **RAGデータ活用**: ステップ④で抽出した山本さんの具体的な数字・エピソード・実体験を必ず織り込む
2. **話し言葉**: すべて話し言葉。「〜ですね」「〜じゃないですか」「めちゃめちゃ」「ぶっちゃけ」等のBRAND DNA口調を使う
3. **数字の裏付け**: 具体的な数字を最低8箇所に含める
4. **二部構成**: 前半＝考え方・フレームワーク、後半＝実践・How-to
5. **離脱防止**: 5分ごとに中間まとめ・つなぎフレーズを入れる
6. **禁止表現**: 「簡単に稼げます」系の煽り、根拠のない断言、説明なしの横文字専門用語、ネガティブな締め、競合名指し否定

### 品質チェック（生成後・表示前に必ず実施）

プロンプトファイルの品質チェックリスト21項目を確認する:
- 合計文字数が9,000〜10,000文字か
- フック冒頭3秒に数字 or 結論が入っているか
- 山本トーンが自然に入っているか
- 具体的な数字が最低8箇所に含まれているか
- 前半＝考え方、後半＝実践の二部構成か
- 中間まとめが2回（5分・13分地点）入っているか
- 体験談が11〜13分に挿入されているか
- Q&Aで3〜4問に回答しているか
- [テロップ案][画面切替指示]が全セクションにあるか
- Shorts台本3本が出力されているか
- その他チェックリスト項目

**チェックで不備があれば、ユーザーに見せる前に自動修正すること。**

### 確認と保存

4. 生成した台本を表示した上で、AskUserQuestionツールで確認:
  question: "YouTube台本です。いかがですか？"
  header: "台本確認"
  options:
    - label: "OK"
      description: "このまま完了します"
    - label: "一部修正したい"
      description: "修正したいセクションを教えてください"
    - label: "全部作り直し"
      description: "トーンや切り口を変えて再生成します"
    - label: "別テーマでもう1本"
      description: "別のテーマで追加の台本を生成します"

5. ユーザーが「OK」または「一部修正したい」で確定した後、台本をマークダウンファイルとして保存する:
   - **保存先**: `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/scripts/`
   - **ファイル名**: `YYMMDD_テーマの短縮英語名_yt.md`（例: `260416_ai-gaichu-smart-keiei_yt.md`）
   - YYMMDDは台本生成日（今日の日付）
   - scriptsフォルダが存在しない場合は自動作成する
   - 保存後、ファイルパスをユーザーに表示する

6. **使用履歴の更新（必須・保存直後に実行）:**

   ステップ④-2で採用した事例ファイル（実際に固有名詞・数字・エピソードを台本本文に反映したファイルのみ）について、以下を実施する。取得フェーズで読んだだけで反映していないファイルは対象外。

   **6-1. 使用済みファイルを `used/` へ移動**
   ```bash
   # 各採用ファイルに対して実行（すでに used/ 配下にある再利用ケースでは省略してよい）
   mv "/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/{採用ファイル}.txt" \
      "/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/used/{採用ファイル}.txt"
   ```

   **6-2. `_USAGE_LOG.md` にNo.付きで追記**
   - Readで `山本さん/used/_USAGE_LOG.md` を読み、既存の最大No.を把握（初回は0）
   - Editツールで末尾に以下のエントリを追記（採用ファイル全件を列挙）:

   ```markdown

   ## No.{最大No.+1} | {YYMMDD} | YouTube
   - **投稿テーマ**: {テーマ}
   - **成果物**: scripts/{保存ファイル名}_yt.md
   - **使用した事例**:
     - `{採用ファイル名A}.txt`（SECTION 6体験談）→ 使った切り口: {1行}
     - `{採用ファイル名B}.txt`（SECTION 8実践）→ 使った切り口: {1行}
   - **メモ**: {再利用時の注意を1〜2行}
   ```

   **※ X記事スキルから連携してきた場合（同一セッションで記事と台本を両方生成）:**
   - X記事側でNo.Xの記録がすでに追加されている場合、台本用には **次の番号No.X+1** で新規エントリを作る
   - 同じ事例を記事・台本両方で使っていた場合は、重複移動は不要（すでに used/ 配下）だが、**ログエントリは台本分も別No.で記録する**

   **6-3. 完了メッセージ**
   ユーザーに以下を表示:
   ```
   ✅ YouTube台本を保存しました: {ファイルパス}
   📦 使用済みへ移動: {採用ファイル名リスト}
   📝 使用履歴に追記: No.{番号}
   ```

---

## 注意事項

- 各ステップで必ず **AskUserQuestionツール** でユーザーの承認を得てから次に進むこと（テキストで「よろしいですか？」と聞くのではなく、選択肢UIを使う）
- Xリサーチ=OFFの場合、ステップ②③をスキップしてステップ④に進む
- X記事スキルからの連携時は、ステップ①〜④をスキップしてステップ⑤に直行する
- 台本プロンプトファイルは必ずReadツールで読み込んでから台本を生成すること
- 台本は9,000〜10,000文字（話し言葉）、12セクション構成
- すべて話し言葉で書く。書き言葉を混ぜない
- Shorts台本3本（数字型・How-to型・共感型）を必ず同時生成する
- 品質チェックリストを通過してからユーザーに表示すること

### RAGデータ運用の厳守ルール（最重要）

- **取得は広く、反映は絞る**: ステップ④-1で8〜12件読んでよいが、④-2で採用する具体事例は **2〜3件（原則2件）**
- **配置を固定**: 体験談（SECTION 6）に1件、実践（SECTION 8）に1〜2件。それ以外のセクションに具体事例を追加投入しない
- **テーマ一貫性が最優先**: 事例を増やすより、1つの事例を深く自然に使う
- **具体的な数字は最低8箇所** の要件は、抽象的な知見・一般論・Xリサーチからの引用でも満たせる。RAG事例を増やして数字を埋める発想をしない
- **クールダウン遵守**: `_USAGE_LOG.md` の末尾10エントリに含まれる事例は原則採用しない
- **使用後は必ず記録**: 反映したファイルは `used/` へ移動し、`_USAGE_LOG.md` にNo.付きで追記
- **永久除外はしない**: `used/` 配下もGrepの検索対象に含める（クールダウンを抜けたものは再利用可）
- **再利用時は角度を変える**: 前回と同じ切り口・論点・表現での再登場は禁止
- **X記事からの連携時**: 記事と台本で同一事例を使う場合でも、角度を変えるか、いずれかで別事例に差し替える
