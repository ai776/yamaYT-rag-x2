---
name: yamamoto-x-post
description: >-
  山本さんRAGデータとX(Twitter)リサーチを組み合わせて、テーマに沿ったX記事（長文投稿）を生成するスキル。
  Use when the user mentions X記事、X投稿、長文投稿、記事作成、山本さん記事、yamamoto article,
  or asks to create X/Twitter long-form articles based on a theme.
---

# 山本さん X記事生成スキル

テーマを受け取り、Xリサーチ（任意）→ 記事構成 → RAG検索 → X記事生成 を行う。
各ステップでユーザーの承認を得てから次に進む。
承認は必ず **AskUserQuestionツール** を使い、選択肢付きで確認すること。

## 設定

- **山本さんフォルダ（現役）**: `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/`
- **使用済みフォルダ**: `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/used/`
- **使用履歴ログ**: `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/used/_USAGE_LOG.md`
- **RAG運用ポリシー**: `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/RAG-POLICY.md`（ステップ④開始時に必ずReadで読み込む）
- **Xリサーチ**: デフォルトON（ユーザーが「Xリサーチなし」「リサーチスキップ」等と言った場合はOFF）

### RAG運用の原則（要約）
1. RAGデータは広く取得（候補8〜12件）、反映は少数に絞る（**X投稿は1〜2件、原則1件**）
2. 1投稿に複数の具体事例を過剰に混在させない。テーマ一貫性が最優先
3. 反映に使った事例は `used/` へ移動し、`_USAGE_LOG.md` にNo.付きで記録
4. `_USAGE_LOG.md` の **末尾10エントリ** で使われた事例は今回の採用候補から降格
5. 永久除外はしない。10投稿を超えたものは再利用可（ただし切り口・論点・表現を変える）

## X API セットアップ（初回のみ）

X APIを利用するには、Pay-Per-Use（従量課金）プランの登録が必要。
2026年2月にFreeプランが廃止され、Pay-Per-Useプランが新設された。

### 前提条件
- 有効なX(Twitter)アカウント
- クレジットカード（最低$5のチャージ）

### 登録手順

**STEP 1: 旧Developer Portalでアカウント作成 & Bearer Token取得**
1. https://developer.x.com にアクセスしXアカウントでサインイン
2. アカウント名・利用目的（250文字以上）を入力して送信
3. 「Projects & Apps」から自分のAppの「Keys and tokens」タブを開く
4. 「Bearer Token」セクションの「Generate」をクリックしてトークンをコピー

**STEP 2: 新Developer ConsoleでPay-Per-Useプランに切り替え**
5. https://console.x.com にアクセス（旧Portalとは別の画面）
6. 左メニュー「アプリ」を開く
7. STEP 1で作ったAppが「Free」になっている場合、**Pay-Per-Useに変更**する
   ※ Freeのままだと検索APIで「403 Client Forbidden」エラーになる

**STEP 3: クレジット購入 & 支出上限設定**
8. 左メニュー「請求書作成」→「クレジット」を開く
9. 「クレジットを購入する」から最低$5（約750円）をチャージ
10. **「支出上限を管理」ボタンから支出上限を$5に設定**（重要！）
    ※ デフォルトは「無制限」。設定しないとクレジット超過分が追加請求される

**STEP 4: 環境変数の設定**
11. ターミナルで以下を実行:
```bash
echo 'export X_API_BEARER_TOKEN="ここにSTEP 1でコピーしたトークンを貼り付け"' >> ~/.bashrc
source ~/.bashrc
```

**STEP 5: 動作確認**
12. ターミナルで以下を実行し、トークンが表示されればOK:
```bash
echo $X_API_BEARER_TOKEN
```

### 参考リンク
- 旧Developer Portal: https://developer.x.com
- 新Developer Console: https://console.x.com
- 詳しい手順ガイド: https://www.and-and.co.jp/gas-lab/x-twitter-api-start/
- 503エラー解決（Free→Pay-Per-Use移行）: https://www.and-and.co.jp/gas-lab/x-twitter-api-503-error/

> **料金目安（Pay-Per-Use）:** 検索・データ取得は投稿より安価。支出上限を必ず設定すること。
> 設定しないとクレジット超過分が追加請求される可能性あり。

---

## ステップ① テーマ受け取り & 初回セットアップ案内

ユーザーからテーマを受け取る。
最初に X API の設定状況をチェックし、未設定なら案内を表示する。

**やること:**

### 1. X API セットアップチェック
Bashツールで `source ~/.bashrc 2>/dev/null; echo $X_API_BEARER_TOKEN` を実行し、環境変数の設定状況を確認する（シェル起動時に読み込まれない場合があるため、必ず `source ~/.bashrc` してから確認すること）。

**未設定の場合、以下の案内を表示する:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  X API セットアップのご案内（任意・初回のみ）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

X APIを設定すると、Xの最新投稿をより正確にリサーチできます。
未設定でも動作しますが、リサーチ精度が下がる場合があります。

【設定する場合の手順】（所要時間: 約10分）

  STEP 1: 旧Developer Portalでアカウント作成 & Bearer Token取得
  https://developer.x.com
  → サインイン → 利用目的を入力（250文字以上）→ 送信
  → 「Projects & Apps」→ 自分のApp → 「Keys and tokens」タブ
  → 「Bearer Token」の「Generate」→ トークンをコピー

  STEP 2: 新Developer ConsoleでPay-Per-Useプランに切り替え
  https://console.x.com （旧Portalとは別の画面です）
  → 左メニュー「アプリ」→ Appが「Free」なら「Pay-Per-Use」に変更
  ※ Freeのままだと検索APIで403エラーになります

  STEP 3: クレジット購入 & 支出上限設定
  → 左メニュー「請求書作成」→「クレジット」
  → 「クレジットを購入する」→ 最低 $5（約750円）をチャージ
  → 「支出上限を管理」→ 上限を $5 に設定（重要！デフォルトは無制限）

  STEP 4: ターミナルで環境変数を保存
  echo 'export X_API_BEARER_TOKEN="STEP1でコピーしたトークン"' >> ~/.bashrc
  source ~/.bashrc

  STEP 5: 動作確認
  echo $X_API_BEARER_TOKEN
  → トークンが表示されればOK

【参考リンク】
  詳しい手順: https://www.and-and.co.jp/gas-lab/x-twitter-api-start/
  503エラー解決: https://www.and-and.co.jp/gas-lab/x-twitter-api-503-error/

【今は設定しない場合】
  → そのまま進めます（WebSearch + Nitter でリサーチします）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

```

→ AskUserQuestionツールで確認:
  question: "X APIを設定しますか？"
  header: "X API"
  options:
    - label: "設定する"
      description: "X Developer Portalで設定を行います（約10分）"
    - label: "今はスキップ"
      description: "WebSearch + Nitter でリサーチします（APIなしでも動作します）"

**設定済みの場合:**
```
X API: 設定済み（フル機能でリサーチできます）
```

### 2. テーマとリサーチ設定の確認
1. テーマを確認する
2. テーマ・Xリサーチ設定・X API状態をまとめて表示した上で、AskUserQuestionツールで確認:
  question: "テーマ「{テーマ}」/ Xリサーチ: {ON/OFF} / X API: {設定済み/未設定} で進めますか？"
  header: "テーマ確認"
  options:
    - label: "このまま進める（推奨）"
      description: "上記の設定でステップ②に進みます"
    - label: "テーマを変更"
      description: "別のテーマに変更します"
    - label: "Xリサーチをオフに"
      description: "Xリサーチを省略してRAG検索から始めます"

**承認を得てから次へ進む。**

---

## ステップ② Xで最新情報をリサーチ

> Xリサーチ=OFFの場合はこのステップをスキップし、ステップ③も省略してステップ④へ進む。

**やること:**

3段階のフォールバック方式でXの最新情報を収集する。
各Phaseで十分な結果（具体的な投稿内容を含む3件以上）が得られたら、次のPhaseはスキップする。

### Phase 1: WebSearchで検索（無料）
1. WebSearchツールを使い、以下の検索クエリでXの最新投稿・トレンドを調査する:
   - `{テーマ} site:x.com`
   - `{テーマ} Twitter トレンド`
   - テーマに関連するキーワードの組み合わせ（2〜3パターン）
2. **判定:** 具体的な投稿内容を含む結果が3件以上 → Phase 2, 3をスキップ

### Phase 2: Nitterでフォールバック補完（無料）
3. Phase 1の結果が不十分な場合、Nitter経由で追加リサーチする:
   - WebFetchツールで `https://xcancel.com/search?q={テーマ}&f=tweets` を取得
   - 取得できない場合は `https://twiiit.com/search?q={テーマ}&f=tweets` を試す
   - それでも取得できない場合は `https://nitter.privacydev.net/search?q={テーマ}&f=tweets` を試す
4. **判定:** Phase 1 + 2 の合計で3件以上 → Phase 3をスキップ

> **Note:** Nitterインスタンスは不安定なため、全インスタンスがダウンしている場合がある。

### Phase 3: X API で最終補完（従量課金）
5. Phase 1 + 2 でも情報が不十分な場合、X APIで検索する:
   - 環境変数 `X_API_BEARER_TOKEN` が設定されているか確認
   - 未設定の場合: ユーザーに「X APIが未設定のため、現在の情報で進めます。APIを使いたい場合はセットアップ手順を参照してください」と通知し、手持ちの情報で進める
   - 設定済みの場合: 以下のコマンドでツイート検索を実行
   ```bash
   curl -s -H "Authorization: Bearer ${X_API_BEARER_TOKEN}" \
     "https://api.x.com/2/tweets/search/recent?query={テーマ}&max_results=20&tweet.fields=created_at,public_metrics,text" \
     | python3 -c "
   import json, sys
   data = json.load(sys.stdin)
   for tweet in data.get('data', []):
       print(f\"[{tweet.get('created_at','')}] {tweet['text']}\")
       metrics = tweet.get('public_metrics', {})
       print(f\"  likes: {metrics.get('like_count',0)}, RT: {metrics.get('retweet_count',0)}\")
       print()
   "
   ```
   - エンゲージメント（いいね・RT数）が高い投稿を優先的にピックアップする

6. 全Phaseの結果を統合し、以下のフォーマットでまとめる:

```
## Xリサーチ結果

情報ソース: {WebSearch / Nitter / X API}（使用したPhaseを記載）

### トレンド・話題
- {箇条書きで3〜5件}

### 注目の意見・切り口
- {箇条書きで3〜5件}

### 使えそうなフレーズ・表現
- {箇条書きで3〜5件}

### 高エンゲージメント投稿（X API使用時のみ）
- {いいね・RT数が多い投稿の要約}
```

7. 情報ソースを明記する（どのPhaseで取得したか）
8. リサーチ結果を表示した上で、AskUserQuestionツールで確認:
  question: "Xリサーチ結果を確認してください。このまま進めますか？"
  header: "リサーチ確認"
  options:
    - label: "このまま進める（推奨）"
      description: "この結果をベースにステップ③へ進みます"
    - label: "追加で調べてほしい"
      description: "キーワードや切り口を追加してリサーチし直します"
    - label: "リサーチ結果を使わない"
      description: "Xリサーチを省略してRAG検索のみで進めます"

**承認を得てから次へ進む。**

---

## ステップ③ 記事構成に整理

**やること:**
1. ステップ②のリサーチ結果を、RAGに投げやすい記事構成に整理する:

```
## 記事構成

### テーマ: {テーマ}

### 背景・トレンド
{Xリサーチから得たトレンド情報を2〜3文で要約}

### 主要な論点
1. {論点1}
2. {論点2}
3. {論点3}

### ターゲットの悩み
- {悩み1}
- {悩み2}
- {悩み3}

### 期待されるメッセージの方向性
{どういう切り口で記事を書くべきかの方向性}
```

2. 記事構成を表示した上で、AskUserQuestionツールで確認:
  question: "この記事構成でRAGに投げますか？"
  header: "構成確認"
  options:
    - label: "このまま進める（推奨）"
      description: "この構成でRAG検索を実行します"
    - label: "構成を修正"
      description: "論点やターゲットの悩みを調整します"
    - label: "構成をスキップ"
      description: "リサーチ結果をそのままRAGに投げます"

**承認を得てから次へ進む。**

---

## ステップ④ 山本さんRAGに投げる（取得フェーズ → 反映フェーズの2段階）

> **重要:** このステップは「取得は広く、反映は絞る」を徹底する。
> 開始時に必ず `山本さん/RAG-POLICY.md` をReadツールで読み込み、内容に従うこと。

### 4-0. ポリシー読込と使用履歴チェック

1. **Readツール**で `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/RAG-POLICY.md` を読み込む
2. **Readツール**で `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/used/_USAGE_LOG.md` を読み込む
3. ログ末尾の **直近10エントリ** で使用された事例ファイル名を抽出し「クールダウン対象リスト」を作る（初回や10件未満の場合はあるだけでよい）

### 4-1. 取得フェーズ（広く集める）

目的: テーマに沿った山本さんの知見・引き出しを幅広く把握する。

**具体的な手順:**
1. Grepツールでテーマのキーワードを検索する。検索対象は以下の **両方**:
   - `山本さん/`（現役ファイル）
   - `山本さん/used/`（アーカイブも含める。永久除外しないため）
2. 関連性の高いファイルを **8〜12件** 読み込む
3. 抽象的な知見・方向性の材料として自由に参照してよい
4. 以下のフォーマットで「候補プール」を作成:

```
## RAG候補プール（取得フェーズ）

### 参照した知見・方向性（自由に活用してよい）
- {テーマに関する山本さんの考え方を3〜5件}

### 具体事例の候補リスト（反映フェーズで絞り込む）
| 候補No | 元ファイル | 事例の要点 | クールダウン | テーマ整合度 |
|--------|-----------|-----------|-------------|-------------|
| 1 | {ファイル名}.txt | {数字・固有名詞を含む1行要約} | {該当あり／なし} | {高／中／低} |
| 2 | ... | ... | ... | ... |
| ... | ... | ... | ... | ... |
```

- **クールダウン**: 4-0で抽出した直近10エントリに含まれるファイルは「該当あり」と明記
- **テーマ整合度**: Xリサーチ結果（ある場合）と照らし、テーマに最も沿うものを「高」とする

### 4-2. 反映フェーズ（少数に絞る）

目的: 1投稿にねじ込む具体事例を **1〜2件（原則1件）** に厳選する。

**具体的な手順:**
1. 候補プールから、以下の優先順で採用候補を並び替える:
   - 第1優先: クールダウン該当なし × テーマ整合度「高」
   - 第2優先: クールダウン該当なし × テーマ整合度「中」
   - 第3優先: クールダウン該当あり だが テーマ整合度「高」で代替不可の場合のみ（切り口を変える前提）
2. 上位 **1〜2件** を「反映候補」として選ぶ（原則1件。2件目は1件目だけでは不足する場合のみ）
3. 以下のフォーマットでまとめる:

```
## RAG反映プラン

### 採用する具体事例（1〜2件）
1. **{ファイル名}.txt**
   - 使う切り口: {テーマに対してどう位置づけるか}
   - 使う数字・固有名詞: {具体的に書き起こす}
   - クールダウン: {該当なし／該当あり（前回使用: No.X, 切り口を変える旨）}

### 記事に使えるキーメッセージ（抽象側・引き出しから）
- {メッセージ1}
- {メッセージ2}
- {メッセージ3}

### 今回「採用しない」理由を記録した候補
- {ファイル名}.txt: {採用しない理由。例: クールダウン該当 / テーマとズレる}
```

4. 反映プランを表示した上で、AskUserQuestionツールで確認:
  question: "採用する具体事例は {件数} 件の予定です。この絞り込みで記事を作成しますか？"
  header: "反映事例の確認"
  options:
    - label: "このまま進める（推奨）"
      description: "この1〜2件に絞って記事を生成します"
    - label: "別の事例に差し替え"
      description: "候補プールから別のファイルを採用し直します"
    - label: "事例なしで書く"
      description: "抽象的な知見のみで記事を作成します（具体事例は温存）"
    - label: "もっと候補を集めたい"
      description: "取得フェーズに戻り、別キーワードで追加検索します"

**承認を得てから次へ進む。**

---

## ステップ⑤ X記事を生成

**やること:**
1. ステップ②〜④で集めた情報をすべて統合する
2. 以下のプロンプトに従い、X記事（長文投稿）を1本生成する:

[prompt]
あなたは山本智也として、X（Twitter）の「記事」機能で公開する長文記事を1本作成しなさい。
以下のルール・構成・文体を厳守すること。

# ルール

## 文体・トーン
- タメ口で、自然な独り言調。「〜だよね」「〜なんですよね」「〜だから」
- 親しみやすいが、中身は本質的でロジカル
- 「笑」「マジで」「マジです」など感情表現を要所に入れる
- 読者に語りかけるように書く（「あなた」「これ分かる？」「聞いたことない？」）
- ハッシュタグ禁止。絵文字禁止。

## 分量
- 2,000〜3,000文字程度（セクション7〜12個）
- 短すぎず、長すぎず。各セクションは100〜300文字程度

## フォーマット
- セクション区切りは `## 見出しテキスト` の形式（例: `## ■ 僕が学んだこと`）。`##` だけの行や `---` 水平線は使わない
- 見出しは `## ■{見出し}` または `## {番号}）{見出し}` のどちらかで統一
- 箇条書きは `-` リストまたは `>` 引用ブロックを使い分ける
- フロー図的な表現は `>` 引用ブロック + `↓` で表現する
- 太字・下線は使わない

# 記事の構成テンプレート

## パート1: タイトル＆導入（冒頭）
- 1行目: `# タイトル`（見出し1。キャッチーで短く、刺さる一言）
- **重要: ユーザーから与えられたテーマをそのままタイトルにしない。** テーマを元に、記事の内容に合った独自のタイトルを考える。「└」などの装飾文字も使わない
- 空行のあと、2〜5行で「共感」or「問いかけ」or「結論先出し」
- 読者が「続きを読みたい」と思う引き込み
- 最後に「↓」や「今日はその話をします。」で本題へ誘導

## パート2〜N: 本題セクション（7〜10個）
各セクションは以下のいずれかのパターンで構成する:

### パターンA: 主張→具体例→教訓
- 見出しで主張を端的に述べる
- 具体例（実体験・数字・固有名詞）で裏付け
- 最後に教訓・学びを1〜2行で

### パターンB: 問いかけ→回答→深掘り
- 読者が持ちそうな疑問を見出しに
- 回答を簡潔に
- さらに深掘り or 例外 or 注意点を追加

### パターンC: 対比→結論
- 2つの選択肢を対比（普通の人 vs 成功者、Taker vs Giver など）
- どちらが正解かを明確に
- なぜそうなるかの理由

### パターンD: 箇条書きまとめ
- 具体的な手法・事例・選択肢をリストで提示
- 読者が「自分ならこれだ」と選べる形に

## パートN+1: まとめ
- 記事全体の要点を「方程式」or「箇条書き」で総括
- 5〜8個の箇条書きが理想
- 簡潔に、繰り返しにならないように

## パートN+2: CTA（最後）
- 「もし〜なら、DMしてください」形式
- 押し付けず、でも行動を促す
- 最後の1行は余韻を残す短い一言で締める

# 重要：記事の質を決める要素

1. **具体性**: 抽象論だけで終わらない。数字・固有名詞・実体験を必ず入れる
2. **テンポ**: 1〜2行で改行。ダラダラ書かない。短文の連打でリズムを作る
3. **意外性**: 読者の「常識」を一度否定してから、新しい視点を提示する
4. **実用性**: 読んだ後に「何をすればいいか」が明確に分かる
5. **山本さんらしさ**: RAGから得た具体的な数字・エピソード・実体験を織り込む

[/prompt]

3. 生成した記事を表示した上で、AskUserQuestionツールで確認:
  question: "X記事です。いかがですか？"
  header: "記事確認"
  options:
    - label: "OK"
      description: "このまま完了します"
    - label: "一部修正したい"
      description: "修正したいセクションを教えてください"
    - label: "全部作り直し"
      description: "トーンや切り口を変えて再生成します"
    - label: "別テーマでもう1本"
      description: "別のテーマで追加の記事を生成します"
    - label: "YouTube台本も作る"
      description: "同じテーマ・リサーチ・RAGデータでYouTube台本を生成します"

4. ユーザーが「OK」または「一部修正したい」で確定した後、記事をマークダウンファイルとして保存する:
   - **保存先**: `/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/articles/`
   - **ファイル名**: `YYMMDD_テーマの短縮英語名.md`（例: `260406_claude-code-ha-nandemo-dekiru-dakara-tsukawanai.md`）
   - YYMMDDは記事生成日（今日の日付）
   - articlesフォルダが存在しない場合は自動作成する
   - 保存後、ファイルパスをユーザーに表示する

5. **使用履歴の更新（必須・保存直後に実行）:**

   ステップ④-2で採用した事例ファイル（実際に固有名詞・数字・エピソードを記事本文に反映したファイルのみ）について、以下を実施する。取得フェーズで読んだだけで反映していないファイルは対象外。

   **5-1. 使用済みファイルを `used/` へ移動**
   ```bash
   # 各採用ファイルに対して実行（mv=移動、コピーではない）
   # 対象ファイルがすでに used/ 配下にある場合（再利用ケース）は、このmvは省略してよい
   mv "/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/{採用ファイル}.txt" \
      "/Users/teach/environment/app/2507/260417_yamaYT-rag-x-main/山本さん/used/{採用ファイル}.txt"
   ```

   **5-2. `_USAGE_LOG.md` にNo.付きで追記**
   - Readで `山本さん/used/_USAGE_LOG.md` を読み、既存の最大No.を把握（初回は0）
   - Editツールで末尾に以下のエントリを追記（`<!-- エントリはここから下に追記する ... -->` の下にappend）:

   ```markdown

   ## No.{最大No.+1} | {YYMMDD} | X記事
   - **投稿テーマ**: {テーマ}
   - **成果物**: articles/{保存ファイル名}.md
   - **使用した事例**:
     - `{採用ファイル名}.txt` → 使った切り口: {1行でどう使ったか}
   - **メモ**: {再利用時の注意。例: 「金額インパクトで使用。次回は組織論の切り口なら再利用可」}
   ```

   **5-3. 完了メッセージ**
   ユーザーに以下を表示:
   ```
   ✅ 記事を保存しました: {ファイルパス}
   📦 使用済みへ移動: {採用ファイル名}.txt
   📝 使用履歴に追記: No.{番号}
   ```

6. ユーザーが「YouTube台本も作る」を選択した場合:
   - まず記事を保存し、**使用履歴の更新（上記5）も先に完了させる**
   - その後、yamamoto-yt-scriptスキルのステップ⑤（YouTube台本生成）に直行する
   - テーマ・Xリサーチ結果・記事構成・RAG反映プランはすべて会話コンテキストに残っているため、再収集は不要
   - 「X記事作成で収集したリサーチ・RAGデータを引き継ぎます。YouTube台本生成に進みます。」と表示してからステップ⑤を開始する
   - **注意:** X記事で使った事例を台本でも使う場合は、同じ事例を同じ切り口で連続使用しないこと（角度を変える or 別事例に差し替え）

---

## 注意事項

- 各ステップで必ず **AskUserQuestionツール** でユーザーの承認を得てから次に進むこと（テキストで「よろしいですか？」と聞くのではなく、選択肢UIを使う）
- Xリサーチ=OFFの場合、ステップ②③をスキップしてステップ④に進む
- X記事は2,000〜3,000文字、セクション7〜12個を目安に生成する
- ハッシュタグ・絵文字は絶対に使わない
- タメ口で自然な独り言調を維持する
- セクション区切りは `## 見出しテキスト` の形式。`##` だけの行や `---` は使わない。見出しは `■` or 番号付き
- 短い文で改行。1〜2行でテンポよく。ダラダラ書かない
- 最後は必ずCTA（DMしてください）で締める

### RAGデータ運用の厳守ルール（最重要）

- **取得は広く、反映は絞る**: ステップ④-1で8〜12件読んでよいが、④-2で採用する具体事例は **1〜2件（原則1件）**
- **テーマ一貫性が最優先**: 事例を増やすより、1つの事例を深く自然に使う
- **複数の具体事例を過剰に混在させない**: 「ついでにこの事例も」は禁止
- **クールダウン遵守**: `_USAGE_LOG.md` の末尾10エントリに含まれる事例は原則採用しない
- **使用後は必ず記録**: 反映したファイルは `used/` へ移動し、`_USAGE_LOG.md` にNo.付きで追記
- **永久除外はしない**: `used/` 配下もGrepの検索対象に含める（クールダウンを抜けたものは再利用可）
- **再利用時は角度を変える**: 前回と同じ切り口・論点・表現での再登場は禁止
